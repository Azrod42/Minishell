-Recuperatio des inputs user
-Recuperation des arg (=) (ex: ARG="test"); //affichage actif dans le main lors de la comande 'exit'
-Remplacement des $ par la data des arg = //ongoin 
-Split de l'input user dans char **prompt
-Redirect //que l'exit de dispo
-ft_exit //gestion de la sortie du shell sans leaks;

+tout les leaks sont gere sur la branch master

faire la gestion des erreurs des pipes;
faire la gestion des replace d'arg;